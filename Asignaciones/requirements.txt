# requirements.txt
# Dependencias para: scraping (requests + BeautifulSoup), Excel, PDFs y
# automatización COM (Windows). Ya no se usa Selenium/Chrome en ningún script.

requests
beautifulsoup4
lxml
pandas
openpyxl
PyMuPDF
PyPDF2

# Solo Windows (para win32com.client / Dispatch, usado para exportar Excel a PDF)
# Instalar todo con:  python -m pip install -r requirements.txt
pywin32; platform_system == "Windows"